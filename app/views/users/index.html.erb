<div class="container">
	<div class="row">
		<div class="col-md-12">
			<div><% @users.each do |user|%></div>
			<div><%= user.name %></div>
			<div><%= user.profile %></div>
			<div><%= link_to 'user_new', edit_user_path(user)%></div>
			<% end %>
		</div>
	</div>
</div>